# This file controls the configuration of the syslog plugin.
# It allows audit events to be sent to syslog in addition to the audit log,
# making them accessible through standard log management tools.

active = yes
direction = out
path = /sbin/audisp-syslog
type = always
args = LOG_INFO
format = string
